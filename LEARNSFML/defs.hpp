#pragma once
#include<SFML/Graphics.hpp>
#include<iostream>
#include<memory>

const float paddleSpeed = 300.0f;
const sf::Vector2f default_paddle_size(20.0f, 150.0f);
const sf::VideoMode Vmode(1280, 720);
const unsigned int frameRate = 240u;
const sf::Color color(255, 0, 255, 255);

