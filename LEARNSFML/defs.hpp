#pragma once
#include<SFML/Graphics.hpp>


const float paddleSpeed = 1.0f;
const sf::Vector2f default_paddle_size(10.0f, 100.0f);
const sf::VideoMode Vmode(800, 600);
const sf::Color color(0, 255, 0, 255);